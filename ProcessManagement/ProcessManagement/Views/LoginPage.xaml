<?xml version="1.0" encoding="utf-8" ?>
<ContentPage
    x:Class="ProcessManagement.Views.LoginPage"
    xmlns="http://xamarin.com/schemas/2014/forms"
    xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
    xmlns:d="http://xamarin.com/schemas/2014/forms/design"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    Shell.NavBarIsVisible="False"
    Visual="Material"
    mc:Ignorable="d">
    <ContentPage.Content>
        <Grid>
            <Grid.RowDefinitions>
                <RowDefinition Height="30*" />
                <RowDefinition Height="70*" />
            </Grid.RowDefinitions>
            <StackLayout Grid.Row="0" VerticalOptions="Center">
                <Label FontSize="Medium" HorizontalTextAlignment="Center" Text="Welcome to Process Management App" VerticalTextAlignment="Center" />
                <Label FontSize="Medium" HorizontalTextAlignment="Center" Text="Please enter your Username and password." VerticalTextAlignment="Center" />
            </StackLayout>

            <StackLayout Grid.Row="1" Padding="20">
                <Entry Placeholder="Username" Text="{Binding UserName}" />
                <Entry Placeholder="Password" Text="{Binding Password}" />
                <Button x:Name="btnLogin" Clicked="loginClick" CornerRadius="20" Text="Login" VerticalOptions="End" />
            </StackLayout>
        </Grid>
    </ContentPage.Content>
</ContentPage>